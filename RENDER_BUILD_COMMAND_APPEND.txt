# Add this to the END of your existing Render build command
# (after the ledger build section)

 && if [ -d "anti_default" ]; then export PATH="/opt/render/project/nodes/node-${NODE_VERSION:-22.16.0}/bin:$PATH" && cd anti_default && npm install && NODE_ENV=production STATIC_EXPORT=true BASE_PATH=/anti-default npm run build && test -f out/index.html && cd .. && echo "✅ Anti-Default build completed"; fi
