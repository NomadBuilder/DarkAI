# Copy this ENTIRE command into Render Dashboard → Settings → Build Command

pip install --disable-pip-version-check --upgrade pip && pip install --disable-pip-version-check -r requirements.txt && if [ -d "ledger" ]; then export PATH="/opt/render/project/nodes/node-22.16.0/bin:$PATH" && cd ledger && rm -rf .next out node_modules && npm install --include=dev && NODE_ENV=production BASE_PATH=/ledger STATIC_EXPORT=true npm run build && cd .. && echo "✅ Ledger build completed" || echo "❌ Ledger build failed"; fi
