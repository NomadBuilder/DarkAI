# Combined requirements for DarkAI consolidated platform
# Includes dependencies from PersonaForge, BlackWire, and ShadowStack

beautifulsoup4==4.12.2  # Used by dfacesearch for web scraping
dnspython==2.4.2
flask==3.0.0
flask-cors==4.0.0
google-search-results==2.4.2  # SerpAPI client
gunicorn==21.2.0
lxml>=5.0.0
neo4j==5.15.0
openai>=1.0.0
phonenumbers==8.13.27
# Playwright is optional - code gracefully handles if not available
# Uncomment if you need headless browser scraping:
# playwright>=1.48.0  # Newer version supports greenlet>=3.1.0
psycopg2-binary==2.9.9
pytest==7.4.3
pytest-cov==4.1.0
python-dateutil==2.8.2
python-dotenv==1.0.0
python-wappalyzer==0.3.1
python-whois==0.8.0
requests==2.31.0
selenium==4.15.2
setuptools>=80.0.0  # Required for python-wappalyzer
flask-swagger-ui==4.11.1  # Swagger/OpenAPI documentation
deepface>=0.0.79  # Face recognition library (free, local)
opencv-python>=4.8.0  # Required for deepface and deepfake detection
tensorflow>=2.13.0  # Required for deepface and EfficientNetV2 deepfake detection (requires Python 3.12 or earlier)
Pillow>=10.0.0  # Required for deepfake detection image processing
numpy>=1.24.0  # Required for deepfake detection (artifact analysis)
keras-efficientnet-v2>=0.1.0  # EfficientNetV2 models for deepfake detection (optional, falls back to tf.keras if not available)
# Note: Deepfake detection works with artifact analysis (no TensorFlow needed) but EfficientNetV2 requires Python 3.12 or earlier
whoisit>=0.5.0  # RDAP lookups for BlackWire (modern alternative to WHOIS)
