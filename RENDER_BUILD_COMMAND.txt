# Copy this into Render Dashboard → Settings → Build Command

pip install --disable-pip-version-check --upgrade pip && pip install --disable-pip-version-check -r requirements.txt && if [ -d "ledger" ]; then export PATH="/opt/render/project/nodes/node-22.16.0/bin:$PATH" && cd ledger && npm install --include=dev && NODE_ENV=production BASE_PATH=/ledger STATIC_EXPORT=true npm run build && cd ..; fi
