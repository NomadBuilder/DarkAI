# Get involved form → Google Apps Script web app URL (see ledger/GET_INVOLVED_GOOGLE_SHEET.md)
# On Render: set either name; Flask serves it at runtime via /api/protectont-config (no rebuild required).
# NEXT_PUBLIC_GET_INVOLVED_SUBMIT_URL=https://script.google.com/macros/s/XXXXXXXX/exec
# GET_INVOLVED_SUBMIT_URL=https://script.google.com/macros/s/XXXXXXXX/exec
# GET_INVOLVED_ALERT_EMAIL=protectont@gmail.com

# ProtectOnt /admin-events: optional Bearer for scripted saves (page saves work without this)
# PROTECTONT_EVENTS_ADMIN_TOKEN=optional-for-api-scripts
# PROTECTONT_EVENTS_DISABLE_SAVE=1  # emergency kill switch

# ProtectOnt yard-sign delivery routing
# Default emails live in ledger/data/sign-territories.json; override per lead in admin
# (Areas & organizers tab) — saved to instance/sign-lead-emails.json on the server.
# Optional env overrides still work:
# SIGN_LEAD_EMAIL_DAWN=...
# SIGN_LEAD_EMAIL_MARY_JANE=...
# SIGN_LEAD_EMAIL_MARTHA=...
# SIGN_LEAD_EMAIL_EMILY=...
# SIGN_DELIVERY_ALERT_EMAIL=protectont@gmail.com  # fallback when a lead email is not set

# ProtectOnt /admin → Sign-ups & orders dashboard (Bearer: SUBMISSIONS_ADMIN_TOKEN, POSTER_ADMIN_TOKEN, or SECRET_KEY)
# SUBMISSIONS_ADMIN_TOKEN=optional-override
# STRIPE_SECRET_KEY=sk_live_...  # already on Render for poster checkout; powers payments tab

# ProtectOnt /form-admin: live-save join form copy JSON
# PROTECTONT_FORM_DISABLE_SAVE=1  # emergency kill switch
# Email alerts use Google Apps Script MailApp by default. Set true only with a verified Resend domain:
# GET_INVOLVED_USE_RESEND=false

# Email Configuration for Contact Form + Join confirmations
# Option 1: Resend (Recommended - Free tier: 3,000 emails/month)
# Get your API key at: https://resend.com/api-keys
# Use a verified domain From address in production (not onboarding@resend.dev).
RESEND_API_KEY=re_xxxxxxxxxxxxx
FROM_EMAIL=noreply@yourdomain.com
CONTACT_EMAIL=aazirmun@gmail.com

# Join form: confirmation to joiner + follow-up if they don't open/click/confirm
# JOIN_FOLLOWUP_HOURS=48
# JOIN_EMAIL_CRON_TOKEN=long-random-string   # Bearer for /api/protectont/join-email-followups
# Optional: Resend webhook → POST https://protectont.ca/api/protectont/resend-webhook (email.opened, email.clicked)
# Enable open/click tracking on your Resend domain dashboard.

# Option 2: SMTP (Alternative - requires app password)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# CONTACT_EMAIL=aazirmun@gmail.com
